package main.java.pl.meszszi.light_riders;

/**
 * Enum type for denoting whether a player is controlled by us (Hero type) or by our opponent (Enemy type).
 */

public enum PlayerType {
    Hero,
    Enemy
}
